<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.uark.finalproject">

    <!--
     The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
     Google Maps Android API v2, but you must specify either coarse or fine
     location permissions for the "MyLocation" functionality.
    -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo5"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo5"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChildPickupApp">
        <activity
            android:name=".LoginActivity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".ForgotPasswordActivity.ForgotPasswordActivity"
            android:exported="true" />
        <activity
            android:name=".SignupActivity.SignupActivity"
            android:exported="true" />
        <activity
            android:name=".DashboardActivity.DashboardActivity"
            android:exported="true" />
        <activity
            android:name=".ParentProfileActivity.ParentProfileActivity"
            android:exported="true" />
        <activity
            android:name=".ParentProfileActivity.AddParentActivity"
            android:exported="true" />

        <activity
            android:name=".ParentProfileActivity.ViewParentActivity"
            android:exported="true" />

        <activity
            android:name=".ChildProfileActivity.ChildProfileActivity"
            android:exported="true" />

        <activity
            android:name=".ChildProfileActivity.AddChildActivity"
            android:exported="true" />

        <activity
            android:name=".ChildProfileActivity.ViewChildActivity"
            android:exported="true" />
        <activity
            android:name=".VehicleProfileActivity.VehicleProfileActivity"
            android:exported="true" />

        <activity
            android:name=".VehicleProfileActivity.AddVehicleActivity"
            android:exported="true" />

        <activity
            android:name=".VehicleProfileActivity.ViewVehicleActivity"
            android:exported="true" />

        <activity
            android:name=".SchedulePickupActivity.SchedulePickupActivity"
            android:exported="true" />
        <activity
            android:name=".MapViewActivity.MapsActivity"
            android:exported="true" />
        <activity
            android:name=".ReviewPickupsActivity.ReviewPickupsActivity"
            android:exported="true" />
        <activity
            android:name=".ViewQueueActivity.ViewQueueActivity"
            android:exported="true" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCoiqC_qrGO89OqFc2M2OaV-FHOSbY3Rpc" />

        <receiver
            android:name=".MapViewActivity.GeofenceBroadcastReceiver"
            android:enabled="true"
            android:exported="true"></receiver>
    </application>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>